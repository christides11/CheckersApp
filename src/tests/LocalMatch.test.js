import React from 'react';
import {render, fireEvent, waitFor, screen} from '@testing-library/react';
import '@testing-library/jest-dom';
import LocalMatch, {getInitialBoard} from './../pages/LocalMatch';


test('Player moves at wrong turn', () => {});

test('Player moves at turn', async () => {
	//const testBoard = getInitialBoard;
	//expect('RED').toEqual(getInitialBoard[0][1]['playerType']);

	});